[Unit]
Description=Home Assistant Service
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker-compose -f /opt/homeassistantserver/docker-compose.yml up 
ExecStop=/usr/bin/docker-compose -f /opt/homeassistantserver/docker-compose.yml stop

[Install]
WantedBy=local.target
